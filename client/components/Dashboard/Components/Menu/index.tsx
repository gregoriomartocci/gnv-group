import React, { Fragment } from "react";
import { Box } from "@mui/material";
import { MenuContainer } from "./Styles";

const Menu = () => {
  return <Box sx={MenuContainer}>index</Box>;
};

export default Menu;
